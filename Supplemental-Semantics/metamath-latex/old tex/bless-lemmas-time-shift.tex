% This LaTeX file was created by Metamath on 24-Dec-2023 8:33 PM.
\begin{metadefinition}\blTitle{df-bl.nowrr} {\texttt{now} is a real number} $\mathsf{\msc{now}} \in \mathbb{R}$
\begin{align}
\vdash \mathsf{\msc{now}} \in \mathbb{R}\label{eq:df-bl.nowrr}\tag{df-bl.nowrr}
\end{align}
\end{metadefinition}


\begin{metadefinition}\blTitle{df-bl.ts} {Time-Shift Predicate} ($~\string^~$ not
in $\mw{\varphi}$)
\begin{align}
\vdash ( ( \mc{A} \in \mathbb{Z} \wedge \delta \in \mathbb{R} ) \rightarrow ( (
    \mw{\varphi} ~\string^~ \mc{A} ) \leftrightarrow ( \mw{\varphi} @ (
    \mathsf{\msc{now}} + ( \delta \cdot \mc{A} ) ) ) )
    )\label{eq:df-bl.ts}\tag{df-bl.ts}
\end{align}
\end{metadefinition}


\begin{lemma}\blTitle{bl.tsi} {Time-Shift Predicate inference} ($~\string^~$
not in $\mw{\varphi}$)
\begin{align}
\vdash \mc{A} \in \mathbb{Z}\quad\&\quad \vdash \delta \in
    \mathbb{R}\quad\Rightarrow\quad \vdash ( ( \mw{\varphi} ~\string^~ \mc{A} )
    \leftrightarrow ( \mw{\varphi} @ ( \mathsf{\msc{now}} + ( \delta \cdot \mc{A} )
    ) ) )\label{eq:bl.tsi}\tag{bl.tsi}
\end{align}
\end{lemma}


\begin{proof}
\begin{align}
  1 &&  & \vdash \mc{A} \in \mathbb{Z}&\text{Hyp~tsi.1}\notag%bl.tsi.1
  \\ 2 &&  & \vdash \delta \in \mathbb{R}&\text{Hyp~tsi.2}\notag%bl.tsi.2
  \\ 3 &&  & \vdash ( ( \mc{A} \in \mathbb{Z} \wedge \delta \in \mathbb{R} )
     \rightarrow ( ( \mw{\varphi} ~\string^~ \mc{A} )
     \leftrightarrow ( \mw{\varphi} @ ( \mathsf{\msc{now}}
     + ( \delta \cdot \mc{A} ) ) ) )
     )&(\mbox{\ref{eq:df-bl.ts}})\notag
  \\ 4 &&  & \vdash ( ( \mw{\varphi} ~\string^~ \mc{A} ) \leftrightarrow (
     \mw{\varphi} @ ( \mathsf{\msc{now}} + ( \delta \cdot
     \mc{A} ) ) ) )&(\mbox{\ref{eq:mp2an}},1,2,3)\notag
\end{align}
\end{proof}

\begin{metadefinition}\blTitle{df-bl.tsc} {Time-Shift Expression} ($~\string^~$
not in $A$)
\begin{align}
\vdash ( ( \mc{A} \in \mathbb{C} \wedge \mc{B} \in \mathbb{Z} \wedge \delta \in
    \mathbb{R} ) \rightarrow ( \mc{A} ~\string^~ \mc{B} ) = ( \mc{A} @ ( \mathsf{\msc{now}} +
    ( \delta \cdot \mc{B} ) ) ) )\label{eq:df-bl.tsc}\tag{df-bl.tsc}
\end{align}
\end{metadefinition}


\begin{lemma}\blTitle{bl.tsci} {Time-Shift Expression inference}
($~\string^~$ not in $A$)
\begin{align}
\vdash \mc{A} \in \mathbb{C}\quad\&\quad \vdash \mc{B} \in \mathbb{Z}\quad\&\quad \vdash
    \delta \in \mathbb{R}\quad\Rightarrow\quad \vdash ( \mc{A} ~\string^~ \mc{B} ) = ( A
    @ ( \mathsf{\msc{now}} + ( \delta \cdot \mc{B} ) )
    )\label{eq:bl.tsci}\tag{bl.tsci}
\end{align}
\end{lemma}


\begin{proof}
\begin{align}
  1 &&  & \vdash \mc{A} \in \mathbb{C}&\text{Hyp~tsci.1}\notag%bl.tsci.1
  \\ 2 &&  & \vdash \mc{B} \in \mathbb{Z}&\text{Hyp~tsci.2}\notag%bl.tsci.2
  \\ 3 &&  & \vdash \delta \in \mathbb{R}&\text{Hyp~tsci.3}\notag%bl.tsci.3
  \\ 4 &&  & \vdash ( ( \mc{A} \in \mathbb{C} \wedge \mc{B} \in \mathbb{Z} \wedge \delta
     \in \mathbb{R} ) \rightarrow ( \mc{A} ~\string^~ \mc{B} ) = ( A
     @ ( \mathsf{\msc{now}} + ( \delta \cdot \mc{B} ) ) )
     )&(\mbox{\ref{eq:df-bl.tsc}})\notag
  \\ 5 &&  & \vdash ( \mc{A} ~\string^~ \mc{B} ) = ( \mc{A} @ ( \mathsf{\msc{now}} + ( \delta
     \cdot \mc{B} ) ) )&(\mbox{\ref{eq:mp3an}},1,2,3,4)\notag
\end{align}
\end{proof}

\begin{metadefinition}\blTitle{df-bl.tsdis} {Time-shift  distributes over predicates}
\begin{align}
\vdash ( ( \mc{A} \in \mathbb{Z} \wedge \mc{B} \in \mathbb{Z} ) \rightarrow ( ( (
    \mw{\varphi} ~\string^~ \mc{A} ) ~\string^~ \mc{B} ) \leftrightarrow ( \mw{\varphi}
    ~\string^~ ( \mc{A} + \mc{B} ) ) ) )\label{eq:df-bl.tsdis}\tag{df-bl.tsdis}
\end{align}
\end{metadefinition}


\begin{lemma}\blTitle{bl.tsdisi} {Time-shift distributes over predicates}
\begin{align}
\vdash ( \mc{A} \in \mathbb{Z} \wedge \mc{B} \in \mathbb{Z} )\quad\Rightarrow\quad \vdash
    ( ( ( \mw{\varphi} ~\string^~ \mc{A} ) ~\string^~ \mc{B} ) \leftrightarrow (
    \mw{\varphi} ~\string^~ ( \mc{A} + \mc{B} ) ) )\label{eq:bl.tsdisi}\tag{bl.tsdisi}
\end{align}
\end{lemma}


\begin{proof}
\begin{align}
  1 &&  & \vdash ( \mc{A} \in \mathbb{Z} \wedge \mc{B} \in \mathbb{Z}
     )&\text{Hyp~tsdisi.1}\notag%bl.tsdisi.1
  \\ 2 &&  & \vdash ( ( \mc{A} \in \mathbb{Z} \wedge \mc{B} \in \mathbb{Z} ) \rightarrow
     ( ( ( \mw{\varphi} ~\string^~ \mc{A} ) ~\string^~ \mc{B} )
     \leftrightarrow ( \mw{\varphi} ~\string^~ ( \mc{A} + \mc{B} ) )
     ) )&(\mbox{\ref{eq:df-bl.tsdis}})\notag
  \\ 3 &&  & \vdash ( ( ( \mw{\varphi} ~\string^~ \mc{A} ) ~\string^~ \mc{B} )
     \leftrightarrow ( \mw{\varphi} ~\string^~ ( \mc{A} + \mc{B} ) )
     )&(\mbox{\ref{eq:ax-mp}},1,2)\notag
\end{align}
\end{proof}

\begin{lemma}\blTitle{bl.tsdisci} {Time-shift distributes over values}
\begin{align}
\vdash ( \mc{A} \in \mathbb{Z} \wedge \mc{B} \in \mathbb{Z} \wedge \mc{C} \in \mathbb{C}
    )\quad\Rightarrow\quad \vdash ( ( \mc{C} ~\string^~ \mc{A} ) ~\string^~ \mc{B} ) = ( C
    ~\string^~ ( \mc{A} + \mc{B} ) )\label{eq:bl.tsdisci}\tag{bl.tsdisci}
\end{align}
\end{lemma}


\begin{proof}
\begin{align}
  1 &&  & \vdash ( \mc{A} \in \mathbb{Z} \wedge \mc{B} \in \mathbb{Z} \wedge \mc{C} \in
     \mathbb{C} )&\text{Hyp~tsdisci.1}\notag%bl.tsdisci.1
  \\ 2 &&  & \vdash ( ( \mc{A} \in \mathbb{Z} \wedge \mc{B} \in \mathbb{Z} \wedge \mc{C} \in
     \mathbb{C} ) \rightarrow ( ( \mc{C} ~\string^~ \mc{A} )
     ~\string^~ \mc{B} ) = ( \mc{C} ~\string^~ ( \mc{A} + \mc{B} ) )
     )&(\mbox{\ref{eq:df-bl.tsdisc}})\notag
  \\ 3 &&  & \vdash ( ( \mc{C} ~\string^~ \mc{A} ) ~\string^~ \mc{B} ) = ( \mc{C} ~\string^~ ( \mc{A} +
     \mc{B} ) )&(\mbox{\ref{eq:ax-mp}},1,2)\notag
\end{align}
\end{proof}

\begin{lemma}\blTitle{bl.ts0} {Zero time-shift, predicate}
\begin{align}
\vdash ( ( \mw{\varphi} ~\string^~ 0 ) \leftrightarrow ( \mw{\varphi} @
    \mathsf{\msc{now}} ) )\label{eq:bl.ts0}\tag{bl.ts0}
\end{align}
\end{lemma}


\begin{proof}
\begin{align}
  1 &&  & \vdash 0 \in \mathbb{Z}&(\mbox{\ref{eq:0z}})\notag
  \\ 2 &&  & \vdash \mathsf{\msc{now}} \in
    \mathbb{R}&(\mbox{\ref{eq:df-bl.nowrr}})\notag
  \\ 3 &&  & \vdash ( 0 \in \mathbb{Z} \wedge \mathsf{\msc{now}} \in \mathbb{R}
    )&(\mbox{\ref{eq:pm3.2i}},1,2)\notag
  \\ 4 &&  & \vdash ( ( 0 \in \mathbb{Z} \wedge \mathsf{\msc{now}} \in
    \mathbb{R} ) \rightarrow 
       ( ( \mw{\varphi} ~\string^~
    0 ) \leftrightarrow ( \mw{\varphi} @ (
    \mathsf{\msc{now}} + ( \mathsf{\msc{now}} \cdot 0 ) )
    ) ) )&(\mbox{\ref{eq:df-bl.ts}})\notag
  \\ 5 &&  & \vdash ( ( \mw{\varphi} ~\string^~ 0 ) \leftrightarrow (
    \mw{\varphi} @ ( \mathsf{\msc{now}} + (
    \mathsf{\msc{now}} \cdot 0 ) ) )
    )&(\mbox{\ref{eq:ax-mp}},3,4)\notag
  \\ 6 &&  & \vdash ( \mw{\varphi} \leftrightarrow \mw{\varphi}
    )&(\mbox{\ref{eq:biid}})\notag
  \\ 7 &&  & \vdash \mathsf{\msc{now}} \in
    \mathbb{R}&(\mbox{\ref{eq:df-bl.nowrr}})\notag
  \\ 8 &&  & \vdash ( \mathsf{\msc{now}} \in \mathbb{R} \rightarrow
    \mathsf{\msc{now}} \in \mathbb{C}
    )&(\mbox{\ref{eq:recn}})\notag
  \\ 9 && @9:\  & \vdash \mathsf{\msc{now}} \in
    \mathbb{C}&(\mbox{\ref{eq:ax-mp}},7,8)\notag
  \\ 10 &&  & \vdash ( \mathsf{\msc{now}} \in \mathbb{C} \rightarrow (
    \mathsf{\msc{now}} + 0 ) = \mathsf{\msc{now}}
    )&(\mbox{\ref{eq:addid1}})\notag
  \\ 11 &&  & \vdash ( \mathsf{\msc{now}} + 0 ) =
    \mathsf{\msc{now}}&(\mbox{\ref{eq:ax-mp}},9,10)\notag
  \\ 12 &&  & \vdash \mathsf{\msc{now}} \in \mathbb{C}&(@9)\notag
  \\ 13 &&  & \vdash ( \mathsf{\msc{now}} \in \mathbb{C} \rightarrow (
    \mathsf{\msc{now}} \cdot 0 ) = 0
    )&(\mbox{\ref{eq:mul01}})\notag
  \\ 14 &&  & \vdash ( \mathsf{\msc{now}} \cdot 0 ) =
    0&(\mbox{\ref{eq:ax-mp}},12,13)\notag
  \\ 15 &&  & \vdash 0 = ( \mathsf{\msc{now}} \cdot 0
    )&(\mbox{\ref{eq:eqcomi}},14)\notag
  \\ 16 &&  & \vdash ( \mathsf{\msc{now}} + 0 ) = ( \mathsf{\msc{now}} + (
    \mathsf{\msc{now}} \cdot 0 )
    )&(\mbox{\ref{eq:oveq2i}},15)\notag
  \\ 17 &&  & \vdash \mathsf{\msc{now}} = ( \mathsf{\msc{now}} + (
    \mathsf{\msc{now}} \cdot 0 )
    )&(\mbox{\ref{eq:eqtr3i}},11,16)\notag
  \\ 18 &&  & \vdash ( ( \mw{\varphi} \leftrightarrow \mw{\varphi} ) \wedge
    \mathsf{\msc{now}} = ( \mathsf{\msc{now}} + (
    \mathsf{\msc{now}} \cdot 0 ) )
    )&(\mbox{\ref{eq:pm3.2i}},6,17)\notag
  \\ 19 &&  & \vdash ( ( ( \mw{\varphi} \leftrightarrow \mw{\varphi} ) \wedge
    \mathsf{\msc{now}} = ( \mathsf{\msc{now}} + (
    \mathsf{\msc{now}} \cdot 0 ) ) ) \rightarrow 
       ( (
    \mw{\varphi} @ \mathsf{\msc{now}} ) \leftrightarrow (
    \mw{\varphi} @ ( \mathsf{\msc{now}} + (
    \mathsf{\msc{now}} \cdot 0 ) ) ) )
    )&(\mbox{\ref{eq:df-bl.ateqp}})\notag
  \\ 20 &&  & \vdash ( ( \mw{\varphi} @ \mathsf{\msc{now}} ) \leftrightarrow (
    \mw{\varphi} @ ( \mathsf{\msc{now}} + (
    \mathsf{\msc{now}} \cdot 0 ) ) )
    )&(\mbox{\ref{eq:ax-mp}},18,19)\notag
  \\ 21 &&  & \vdash ( ( \mw{\varphi} ~\string^~ 0 ) \leftrightarrow (
    \mw{\varphi} @ \mathsf{\msc{now}} )
    )&(\mbox{\ref{eq:bitr4i}},5,20)\notag
\end{align}
\end{proof}

\begin{lemma}\blTitle{bl.cd} {Constructive Dilemma.}
\begin{align}
\vdash ( \mw{\varphi} \rightarrow \mw{\psi} )\quad\&\quad \vdash ( \mw{\chi}
    \rightarrow \mw{\theta} )\quad\&\quad \vdash ( \mw{\varphi} \vee \mw{\chi}
    )\quad\Rightarrow\quad \vdash ( \mw{\psi} \vee \mw{\theta}
    )\label{eq:bl.cd}\tag{bl.cd}
\end{align}
\end{lemma}


\begin{proof}
\begin{align}
  1 &&  & \vdash ( \mw{\varphi} \vee \mw{\chi} )&\text{Hyp~cd.3}\notag%bl.cd.3
  \\ 2 &&  & \vdash ( \mw{\varphi} \rightarrow \mw{\psi}
     )&\text{Hyp~cd.1}\notag%bl.cd.1
  \\ 3 &&  & \vdash ( \mw{\chi} \rightarrow \mw{\theta}
     )&\text{Hyp~cd.2}\notag%bl.cd.2
  \\ 4 &&  & \vdash ( ( \mw{\varphi} \vee \mw{\chi} ) \rightarrow ( \mw{\psi}
     \vee \mw{\theta} )
     )&(\mbox{\ref{eq:orim12i}},2,3)\notag
  \\ 5 &&  & \vdash ( \mw{\psi} \vee \mw{\theta}
     )&(\mbox{\ref{eq:ax-mp}},1,4)\notag
\end{align}
\end{proof}

%\begin{metadefinition}\blTitle{df-bl.id} Create Identifier Class; first
%character must be a letter,
%others may be letter or digit; underscore not allowed
%\begin{align}
%\vdash \,\mathrm{ID} = ( \,\mathrm{Word } \,\mathrm{Letter} \,\mathrm{ ++ }
%    \,\mathrm{Word } ( \,\mathrm{Letter} \cup \,\mathrm{Digit} )
%    )\label{eq:df-bl.id}\tag{df-bl.id}
%\end{align}
%\end{metadefinition}


